# Backend configuration (optional — defaults are fine for development)
LBS_PORT=8742
LBS_HOST=127.0.0.1
LBS_DB_PATH=local_biz_scraper.db
# LBS_DATABASE_URL=postgresql://user:pass@localhost:5432/lbs
# LBS_ALLOWED_ORIGINS=http://localhost:5173,https://yourdomain.com

# Frontend configuration (set in .env.production for web builds)
# VITE_API_URL=https://api.yourdomain.com
